import java.util.*;
public class hash{
	public static void main (String args[]){
		/*Here data is not added in indexing  approach.
		We get hashcodes for elements which are being added.*/
		HashSet set=new HashSet();
		set.add(5);
		set.add(4);
		set.add(3);
		set.add(2);
		set.add(1);
		set.add(5);
		set.add("IC");
		/* 1.Data is unique, no redundancy.
		2.Data is unordered in output due to hashing.*/
		System.out.println(set);
		Iterator itr=set.iterator();
		while(itr.hasNext())
			System.out.println(itr.next());
		
		
	}
}
